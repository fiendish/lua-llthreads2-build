name: Build lua-llthreads2 for Win32 static

on:
  workflow_dispatch:
  push:
    branches:
      - main
    tags:
      - '*'

jobs:
  build:
    runs-on: windows-2022
    defaults:
      run:
        shell: pwsh

    steps:
      - name: Checkout lua-llthreads2
        uses: actions/checkout@v3
        with:
          repository: fiendish/lua-llthreads2
          path: lua-llthreads2
          submodules: recursive

      - name: Set up MSVC x86 build environment
        uses: ilammy/msvc-dev-cmd@v1
        with:
          arch: x86

      - name: Clone LuaJIT
        run: git clone -b v2.1 --single-branch --depth=1 https://github.com/fiendish/LuaJIT.git C:\luajit

      - name: Download prebuilt lua51.lib
        run: |
          Invoke-WebRequest 'https://github.com/fiendish/LuaJIT-build/releases/download/latest/lua51.lib' -OutFile 'C:\lua51.lib'

      - name: Build lua-llthreads2
        working-directory: lua-llthreads2
        run: |
          cl /nologo /MT /O2 -c -Fol52util.obj -Ic:\luajit\src c:\lua-llthreads2\src\l52util.c -DLLTHREAD_MODULE_NAME=llthreads2
          cl /nologo /MT /O2 -c -Follthread.obj -Ic:\luajit\src c:\lua-llthreads2\src\llthread.c -DLLTHREAD_MODULE_NAME=llthreads2
          link -dll -export:luaopen_llthreads2 -out:llthreads2.dll lua51.lib l52util.obj llthread.obj kernel32.lib

      - name: Upload build artifact
        uses: actions/upload-artifact@v4
        with:
          name: lua-llthreads2-win32
          path: lua-llthreads2/llthreads2.dll

      - name: Create or update "latest" release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: latest
          name: Latest
          files: lua-llthreads2/llthreads2.dll
          prerelease: false
          draft: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
